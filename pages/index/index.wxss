/* index.wxss

目录：

1 设置整个index页面的背景色

2 这八项是设置content(flex布局的容器),cover,title,author,time,publisher,pages,price的位置

3 标签导航的样式

4 因标签悬浮导致和book-content重叠，所以设置大容器book-list

5 设置标签被点击时的样式

6 设置页面到底部时出现的加载提示的样式

*/



/* 正文 */

/* 1 设置整个index页面的背景色 */
page {
    background-color: #eff3f9;
}


/* 2 这八项是设置content(flex布局的容器),cover,title,author,time,publisher,pages,price的位置 */

.content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    font-family: sans-serif;
    line-height: 1.7;
    color: black;
    width: 515rpx;
    height: 945rpx;
    margin: 13rpx 35rpx 25rpx 70rpx;
    padding: 45rpx;
    background-color: white;
    box-shadow:8rpx 15rpx 8rpx #c3d5f4;

}

.cover {
    width: 200rpx;
    height: 300rpx;
    margin-top: 60rpx;
    border-style: groove;
    box-shadow:8rpx 12rpx 8rpx #c3d5f4;
}


.title {
    font-size: 45rpx;
    margin-top: 60rpx;
}

.author {
    font-size: 35rpx;
    margin-top: 35rpx;

}

.time {
    font-size: 33rpx;
}

.publisher {
    font-size: 33rpx;
    margin-top: 20rpx;
}

.pages {
    font-size: 33rpx;
}

.price {
    font-size: 31rpx;
}


/* 3 标签导航的样式 */
.tag-list {
    position: fixed;
    background-color: white;
    white-space: nowrap;
    width: 100%;
    box-shadow:8rpx 4rpx 8rpx #c3d5f4;
}

.tag-item {
    padding: 10rpx 18rpx;
    margin-left: 4rpx;
    font-family: sans-serif;
    font-size: 32rpx;
    display: inline-block;
    background-color: white;
}

/* 4 因标签悬浮导致和book-content重叠，所以设置大容器book-list */
.book-list {
    padding-top: 60rpx;
}

/* 5 设置标签被点击时的样式 */
.active-tag {
    border-bottom: 10rpx solid #3577f2;
}

/* 6 设置页面到底部时出现的加载提示的样式 */
.loading {
    text-align: center;
    margin: 50rpx;
    color: light;
}
